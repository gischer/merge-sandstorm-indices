{
  "name": "merge-sandstorm-indices",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha",
    "test-watch": "TEST_WATCH=1 meteor test --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.17.9",
    "axios": "^1.7.7",
    "jquery": "^3.6.0",
    "meteor-node-stubs": "^1.2.1",
    "ramda": "^0.30.1"
  },
  "devDependencies": {
    "chai": "^4.5.0"
  }
}
